language: java
sudo: true

jdk:
  - oraclejdk7
  - oraclejdk8

services:
  - docker

after_success:
  - mvn clean verify jacoco:report jacoco:report-integration coveralls:report -Pcoverage -Ddocker.showLogs -Dmaven.build.timestamp.format=yyMMddHHmm
