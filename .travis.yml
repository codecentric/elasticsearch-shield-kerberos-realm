language: java
sudo: true

install: true

jdk:
  - oraclejdk7
  - oraclejdk8

script: mvn clean verify -Ddocker.showLogs -B

services:
  - docker

after_success:
  - bash <(curl -s https://codecov.io/bash)
  - mvn coveralls:report -Dmaven.build.timestamp.format=yyMMddHHmm
